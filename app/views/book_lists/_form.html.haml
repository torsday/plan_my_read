= form_for @book_list do |f|
  - if @book_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@book_list.errors.count, "error")} prohibited this book_list from being saved:"
      %ul
        - @book_list.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
